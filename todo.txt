
For v1.1.2
----------
Check for render recursion

runtime error reporting


Future:
-------
add compilation hooks so external people can add their own generated methods & fields

<%! using webmod %>

Multiple ctx args (optionally give mixin render mixin method? use a map [Str:Type])
